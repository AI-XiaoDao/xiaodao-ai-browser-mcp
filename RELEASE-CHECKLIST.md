# 发布检查清单（Release Checklist）· 小刀 AI 浏览器 MCP Server

> 每次出包**逐条打勾**；任何一条未过都不要发布。所有断言都要求**机对机输出**，不接受人工誊抄。

## 0. 前置
- [ ] 工作源与 `src/` 一致（18 个 `.wsv`）；`git status` 干净，提交信息写明本版改动
- [ ] 版本号已递增（`MCP_常量.MCP_版本号`、文档页 `docs/index.html` 的版本标注、包名/内层目录名**三处一致**）
      —— 历史事故：内层目录名与文档页曾残留上一版号（v3.3.1）

## 1. 编译（双架构）
- [ ] `voldev_awp.exe @compile AI-Fbowser-Mcp.vsln /r /ff <日志>` 退出码 0
- [ ] 日志出现 `编译生成目标文件…成功` 且 **`共遇到了 0 个警告`**（x64 与 x86 各一份日志）
- [ ] 产物新鲜度：exe 修改时间 **晚于** 本次编译开始（防"跑的是上一轮的 exe"）
- [ ] 记录双架构 exe 的 **大小 + MD5**（exe 因链接时间戳不逐字节可复现，MD5 只用于区分构建批次）
- [ ] `.vprj` 等工程文件在编译后被还原（diff 干净）

## 2. 回归（对**副本**跑，不要对着正在服务的运行目录 —— 套件会清理 `mcp_cache.db*`）
- [ ] `.\run-tests.ps1 -Exe <副本\AI-Fbowser-Mcp.exe> -Suite all` → **PASS**
      - `sweep`：348/348 有回执，`no-reply = 0`、`bad-json = 0`
      - `frames`：P1–P8 全过（CL / 裸行 / 管线化 / 帧体空白 / 字面量 `Content-Length:` / NUL → -32700 / CL 失配两向）
      - `transport`：A/B/C 全过（CL 声明小于、大于实际均不永久挂起）
      - `pool`：饱和期 `tools/list` **< 1000ms**（修前实测 11267ms）、`ping` 快答、第 4 条 `tools/call` 不被静默排队
      - `cdp`：CDP 层错误如实回报且注明"不回退重跑"，紧随的正常表达式仍走 CDP（通道未被判死）

## 3. 专项（改动涉及哪块就跑哪块）
- [ ] 大载荷：`execute_js` 返回 >400KB 时被**源头封顶**并如实说明（不崩、不空回）
- [ ] 块读：2MB / 5MB 单行请求 < 1s 且随后小请求正常
- [ ] `max_ms`：显式传值被尊重、默认档受 `max_transport_block_ms` 约束、超限有回帧
- [ ] 缓存库：启动自检通过且**不误判**（正常日志为 `缓存库自检通过(…就绪等待 Nms)`）；隔离产物 ≤ 3 套
- [ ] 诚实性：发生过自动补齐时回执含 `auto_prepared`；未发生时不出现该字段

## 4. 打包与交付完整性
- [ ] 5 个交付 zip 重建；内层**单层**根目录且名字含**本版**版本号
- [ ] `x64 载荷内 exe == 本次编译产物`（机对机断言为 True）
- [ ] `源码包 .wsv 比对 18 个, 一致 18`
- [ ] `校验清单_checksums.csv` 行数 == 交付文件数 − 1（清单不含自身），回读一致 N/N
- [ ] 上一版归档到 `_superseded_<上一版>\`（不是删除）

## 5. 运行环境与发布
- [ ] `AI-Fbowser-Mcp.exe --print-config` 输出**本机真实路径**（无占位符），可直接粘贴给客户端
- [ ] 实机运行目录已切到本版；`ping` 工具回 `version` == 本版号（防"装的是新版、跑的是旧版"）
- [ ] 仓库：源码 + 生成 C++ 已提交；Release 资产名**用 ASCII**（中文名经 PowerShell→argv 会损坏）
- [ ] 审计文档（含本版台账章节）已随仓/随 Release 同步

## 6. 已知边界（发布说明里如实写清，不要含糊）
- [ ] 未做代码签名（SmartScreen 会提示）——若做了签名，此处改为记录证书指纹
- [ ] 非 `tools/call` 的 `initialize` / `workflow_stop` 在全忙期仍入队（有界延迟，不会丢）
- [ ] 缓存库"内容型"毒化的**触发条件不可构造**，只有启发式安全网；真毒化根因未定位
- [ ] stderr 为 GBK，非中文 Windows 控制台会显示乱码
