# 验收事实（机对机）/ Verification evidence

所有数字均来自自动化脚本输出，不接受人工誊抄。

| 项目 | 结果 |
|---|---|
| 编译 | 火山 `voldev_awp` → **0 错误 0 警告**（x64 与 x86 各一次）+ 产物新鲜度断言 |
| 全量工具扫描 | **348/348 有回执**，`no-reply=0`、`bad-json=0`（202 条 `tool-error` 均为空参校验的正常拒绝） |
| 帧族 | P1–P8 全过：CL ×3 / 帧体含空白 / 裸行 ×3 / 参数含字面量 `Content-Length:` / 管线化 / 裸行含 NUL→`-32700` 后仍可服务 / CL 偏大与偏小 → 显式 `-32700` + 优雅退出 |
| 传输 | 基线三连发 + CL 声明小于/大于实际 两向均不永久挂起 |
| 工作池饱和 | `tools/list` **94ms**（修复前 11,267ms）、`ping` 61ms、第 4 条 `tools/call` 不被静默排队 |
| CDP 层诚实性 | 不可序列化值**如实回报**并注明"不会回退重跑"；紧随的正常表达式仍走 CDP（通道未被判死） |
| 客户端矩阵 | `Content-Length`(CRLF) / 仅 LF / 裸行 三种帧风格 × 协议版本 2024-11-05 / 2025-06-18 / 伪造版本 × 丰富握手：**7/7 通过** |
| 浸泡 | 6 分钟 2,512 次作业：**无回包 0 次**；`p50=69ms p95=110ms`；工作集 128→152MB 后**走平**；句柄 1208–1220 震荡无单调增长 |

> 边界：浸泡为**冒烟级**（能发现明显单调增长，不能证明无泄漏）；GUI 客户端矩阵为**协议级**，真实客户端需各自安装实测。