# 安全策略 / Security Policy

## 报告漏洞
请**不要**用公开 Issue 报告安全问题。请通过以下私下渠道联系，并在标题注明 `[SECURITY]`：
- QQ 212577526 ｜ VX XSMZAS1 ｜ 交流群 280742938

请附：版本号（`ping` 工具返回的 `version`）、复现步骤、影响面、可选的 PoC。
我们会在 **7 个工作日**内确认收悉，并在修复后于 Release 说明中署名致谢（如你同意）。

## 支持范围
| 版本 | 状态 |
|---|---|
| 3.3.3（最新） | 接受安全报告并修复 |
| ≤ 3.3.2 | 仅在高危情况下评估；建议先升级 |

## 本产品的攻击面提示（部署方须知）
本服务是**本地 stdio MCP 服务端**，不具备网络监听能力（无 HTTP 端口），但被授权的 MCP 客户端可通过它：
控制浏览器（含执行页面 JS、拦截/改写网络请求、下载文件、读写剪贴板类能力）。
**因此：只把本服务接入你信任的 MCP 客户端；不要在共享/多用户机器上对不受限的用户开放。**
日志（`mcp_console.log`）会落在程序目录，可能包含 URL 与页面信息，请按内部数据分级管理。